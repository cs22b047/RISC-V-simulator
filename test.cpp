// C++ program to words in a sentence.
#include <bits/stdc++.h>
 #include <fstream>

// Driver code
int main()
{
    char* memory =(char*)malloc(100*sizeof(char));
    memory[0] = 'a';
    std::cout<<*memory;
    return 0;
}